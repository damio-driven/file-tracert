{
  "name": "filetracert-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "End-to-end tests: a real Host serving the built SPA, driven through Chromium.",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report .artifacts/report"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "@types/node": "^26.2.0",
    "typescript": "~5.9.2"
  }
}
